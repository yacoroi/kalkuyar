# 🚀 PROJECT BLUEPRINT: SAADET SAHA OPERASYON PLATFORMU

## 1. PROJE VİZYONU VE İŞ MANTIĞI (BUSINESS LOGIC)

**Özet:** Bu proje, klasik siyasi teşkilatlanmayı "Mikro-Hücre" modeline dönüştüren bir saha operasyon yazılımıdır. Amaç sadece veri toplamak değil, üyeyi **harekete geçirmek** ve **davranış kazandırmaktır.**

### Kritik Akış Döngüsü:
INPUT (İçerik) → ACTION (Saha Görevi) → FEEDBACK (Rapor) → LEVEL-UP (Puan)

### Temel Modüller ve Kurallar:

**1. Kişiselleştirilmiş İlgi Alanları:**
* Her üye sisteme girerken "İlgi Alanı" seçmek zorundadır (Örn: Ekonomi, Aile, Gençlik).
* *Kural:* Üyeye sadece seçtiği ilgi alanıyla ilgili görevler ve içerikler gösterilir.

**2. Haftalık "Mikro Görev" Sistemi (Core Feature):**
* Görevler "genel" değil, "uygulanabilir" olmalıdır.
* *Örnek Görev:* "Bu hafta 3 esnafa elektrik faturasını sor."
* **Kritik Kural:** Mevcut görev tamamlanmadan (geri bildirim girilmeden) üyeye yeni görev veya yeni içerik paketi AÇILMAZ. Bu, davranışı tetiklemek içindir.

**3. İçerik Setleri (Söylem Birliği):**
* Her görevle birlikte üyeye bir "Söylem Paketi" sunulur.
* Bu paket şunları içerir:
    * **3 Cümlelik Mesaj:** Sahada söylenecek net mesaj.
    * **Saha Sorusu:** Üyenin karşı tarafa soracağı soru.
    * **Kısa Bilgi:** Konu özeti (PDF/Video).

**4. Gamification (Motivasyon):**
* Sistem bir "CRM" gibi değil, bir "Oyun" gibi hissettirmelidir.
* Puanlar sadece veri girişine değil, görevin zorluğuna göre verilir.
* Geri bildirim (Rapor) girildiği an puan hesaba yansımalı ve "Tebrikler" ekranı çıkmalıdır.

---
*(Dosyanın geri kalanı Teknik Stack ve Veritabanı Şeması olarak aynen devam eder...)*

## 2. TEKNOLOJİ YIĞINI (TECH STACK)

### Mobil Uygulama (Saha Personeli İçin)
* **Framework:** React Native (Expo Managed Workflow) - *Strictly Expo Router.*
* **Dil:** TypeScript.
* **Stil:** NativeWind (Tailwind CSS for React Native).
* **State Management:** Zustand.
* **İkonlar:** Lucide React Native.

### Yönetim Paneli (İl/İlçe Başkanları İçin)
* **Framework:** Next.js (App Router).
* **Stil:** Tailwind CSS.
* **Chart Library:** Recharts (Raporlar için).

### Backend & Veritabanı
* **Platform:** Supabase.
* **Veritabanı:** PostgreSQL.
* **Auth:** Supabase Auth (Email/Password).
* **Security:** RLS (Row Level Security) politikaları zorunludur.

---

## 3. VERİTABANI ŞEMASI (SUPABASE SQL)

AI Agent, lütfen bu şemayı baz alarak veritabanını kur:

```sql
-- 1. PROFİLLER (Üyeler)
create table public.profiles (
  id uuid references auth.users not null primary key,
  full_name text,
  city text,
  district text,
  neighborhood text,
  topics text[], -- Örn: ['Ekonomi', 'Gençlik']
  role text default 'member', -- 'member', 'admin', 'district_head'
  points int default 0,
  avatar_url text,
  created_at timestamp with time zone default now()
);

-- 2. İÇERİK PAKETLERİ (Genel Merkezden gelen haftalık setler)
create table public.content_packs (
  id bigint generated by default as identity primary key,
  week_number int,
  topic text, -- 'Ekonomi'
  title text,
  message_framework text, -- Sahada söylenecek 3 cümle
  survey_questions jsonb, -- Saha soruları: [{q: "Soru 1?", type: "text"}]
  media_url text, -- PDF veya Video linki
  is_active boolean default true,
  created_at timestamp with time zone default now()
);

-- 3. GÖREVLER (Üyelere atanan işler)
create table public.tasks (
  id bigint generated by default as identity primary key,
  user_id uuid references public.profiles(id),
  content_pack_id bigint references public.content_packs(id),
  status text default 'pending', -- 'pending', 'completed'
  assigned_at timestamp with time zone default now(),
  completed_at timestamp with time zone
);

-- 4. SAHA RAPORLARI (Geri Bildirimler)
create table public.reports (
  id bigint generated by default as identity primary key,
  task_id bigint references public.tasks(id),
  user_id uuid references public.profiles(id),
  contact_type text, -- 'Esnaf', 'Komşu', 'Genç', 'Akraba'
  reaction text, -- 'Olumlu', 'Nötr', 'Olumsuz'
  feedback_note text,
  location_lat float,
  location_lng float,
  created_at timestamp with time zone default now()
);
4. UYGULAMA MİMARİSİ VE NAVİGASYON (EXPO)
Uygulama Tab Navigation kullanacak.

A. Auth Flow (Giriş Yapmamış Kullanıcı)
Login Screen: E-posta/Şifre girişi.

Register Screen: Ad Soyad, İl, İlçe seçimi ve İlgi Alanı (Topic) Seçimi (Multi-select).

B. Main Flow (Giriş Yapmış Kullanıcı)
Tab 1: Ana Sayfa (Dashboard)

Üstte: "Hoşgeldin [İsim], Seviye: [Rütbe]"

Ortada: "Haftalık Görevin" kartı (Tıklanabilir).

Altta: Hızlı İşlemler (+ Rapor Gir).

Tab 2: İçerik Kütüphanesi

Seçilen konulara göre geçmiş ve güncel içerik kartları.

Tab 3: Profil & Skor

Kazanılan puanlar, rozetler ve geçmiş raporlarım.

5. GELİŞTİRME KURALLARI (AI TALİMATLARI)
KOD YAPISI: Her zaman modüler çalış. Tek bir dosyaya 500 satır kod yazma. components/ klasörünü aktif kullan.

STİL: Asla StyleSheet.create kullanma. Her zaman NativeWind (Tailwind classları) kullan. Örn: className="flex-1 bg-white p-4".

TİP GÜVENLİĞİ: TypeScript interface'lerini types.ts dosyasında tanımla ve her yerde kullan. any kullanmaktan kaçın.

HATA YÖNETİMİ: Supabase isteklerinde her zaman error kontrolü yap ve kullanıcıya Alert veya Toast ile bilgi ver.

PERFORMANS: Gereksiz re-renderlardan kaçın. Listeler için FlatList kullan.

6. ADIM ADIM YOL HARİTASI (PHASES)
Bu blueprint'i okuyan AI, aşağıdaki sırayla kodlamaya başlamalıdır:

PHASE 1: Setup & Backend: Expo projesini kur, Supabase bağlantısını yap, Tabloları oluştur.

PHASE 2: Auth & Onboarding: Login, Register ve "İlgi Alanı Seçimi" ekranlarını kodla.

PHASE 3: Task Engine: Ana sayfada görev kartını göster, veriyi Supabase'den çek.

PHASE 4: Reporting Flow: Görev detayına girince "Rapor Oluştur" butonu ve Form yapısını kur.

PHASE 5: Gamification: Rapor gönderilince puan artırma mantığını (Trigger veya Frontend logic) kur.

PHASE 6: Admin Panel: Next.js ile web yönetim panelini kur.